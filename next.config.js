module.exports = {
  reactStrictMode: true,
  swcMinify: true,
  i18n: {
    // These are all the locales you want to support in
    // your application
    locales: ['pt-BR', 'en'],
    // This is the default locale you want to be used when visiting
    // a non-locale prefixed path e.g. `/hello`
    defaultLocale: 'pt-BR',
    // This is the locale which will be used when the `Accept-Language` header
    // is sent by the client. This is useful when you want to support multiple
    // languages on the same domain.

  },
}
